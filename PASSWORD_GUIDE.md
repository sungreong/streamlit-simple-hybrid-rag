# 비밀번호 변경 가이드

이 문서는 Streamlit 앱의 비밀번호를 변경하는 방법을 설명합니다.

## 🚀 빠른 방법 (추천)

### 자동 변경 스크립트 사용

```bash
python change_password.py
```

스크립트가 다음을 자동으로 처리합니다:
1. 새 비밀번호 입력 받기
2. SHA-256 해시 생성
3. `.streamlit/secrets.toml` 파일 자동 업데이트

완료 후:
```bash
docker-compose restart
```

---

## 🔧 수동 변경 방법

### 1단계: 비밀번호 해시 생성

```bash
python -c "import hashlib; print(hashlib.sha256('새비밀번호'.encode()).hexdigest())"
```

예시 출력:
```
5e884898da28047151d0e56f8dc6292773603d0d6aabbdd62a11ef721d1542d8
```

### 2단계: secrets.toml 파일 수정

`.streamlit/secrets.toml` 파일을 열고 `password` 값을 변경:

```toml
password = "5e884898da28047151d0e56f8dc6292773603d0d6aabbdd62a11ef721d1542d8"
```

### 3단계: Docker 재시작

```bash
docker-compose restart
```

**주의**: 재빌드는 필요 없습니다! `restart`만 하면 됩니다.

---

## 📋 기본 비밀번호

- **기본 비밀번호**: `admin123`
- **보안을 위해 반드시 변경하세요!**

---

## 🔒 보안 팁

1. **강력한 비밀번호 사용**:
   - 최소 8자 이상
   - 대소문자, 숫자, 특수문자 조합

2. **정기적 변경**:
   - 3-6개월마다 비밀번호 변경 권장

3. **공유 금지**:
   - 비밀번호를 Git에 커밋하지 마세요
   - `.streamlit/secrets.toml`은 `.gitignore`에 포함되어 있습니다

---

## ❓ 문제 해결

### "secrets.toml 파일을 찾을 수 없습니다"

```bash
cp .streamlit/secrets.toml.template .streamlit/secrets.toml
```

### "변경했는데 로그인이 안 됩니다"

1. Docker 재시작 확인:
   ```bash
   docker-compose restart
   ```

2. 해시 값이 올바른지 확인:
   ```bash
   python -c "import hashlib; print(hashlib.sha256('입력한비밀번호'.encode()).hexdigest())"
   ```

3. `secrets.toml` 파일 형식 확인:
   ```toml
   password = "해시값"  # 따옴표 필수!
   ```

---

## 🎯 요약

**가장 쉬운 방법**:
```bash
python change_password.py
docker-compose restart
```

끝! 🎉
